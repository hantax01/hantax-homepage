# 한택스 홈페이지 (hantax-homepage)

한택스(나이스원세무법인 스마트지점)의 대외 홈페이지. GitHub Pages로 배포되며, 도메인 hantax47.com 연결 예정(현재 임시: hantax01.github.io/hantax-homepage).

## 구조
- `index.html` — 메인 (단일 페이지, 섹션: 히어로 → 왜 한택스인가 → 01 세무리포트 → 02 사전알림 → 중간 CTA → 03 고객정보 → 04 사람 → CTA)
- `style.css` — 전 페이지 공용. **수정 시 모든 html의 `style.css?v=N`을 +1** (캐시 무효화)
- `column/` — 세무 칼럼 (index.html 목록 + _template.html 글 템플릿)
- `faq/` — 자주 묻는 질문 (아코디언 + FAQPage JSON-LD)
- `assets/` — 로고·사진·캡처

## 게시 절차
1. 수정 → Edge 헤드리스 캡처로 확인 → `git add <파일 지정> && git commit && git push origin main`
2. push 후 1~2분이면 반영. 커밋 메시지는 한국어 서술형.

## 칼럼 작성 규칙 (AEO)
- 제목 = 사장님이 실제로 묻는 **질문형 문장**
- 첫 블록 = **핵심 답변** (결론부터 3~4문장, 이것만 읽어도 답이 되게)
- 본문 → 근거 법령·예규 명시 → FAQ 2~3개 (본문과 동일 내용을 FAQPage JSON-LD에도)
- `column/_template.html` 복사 → 영문 파일명 → `column/index.html` 목록에 카드 추가(.empty 블록은 첫 글에서 제거)
- 법이 바뀌면 본문과 dateModified 수정

## 문구 원칙 (확정 사항 — 임의 변경 금지)
- 슬로건: **"사장님의 스토리를 기억하는 세무사"**
- 쉬운 한국어 (사장님 눈높이). 컨설팅체 금지: "선제적", "솔루션", "최적화" 류
- **미구현 서비스 게재 금지**: 자금일보·가용현금(개발 중), 상담봇, "매달" 발송 주기, 세금계산서 오발행 검증 — 구현 확인 후에만 추가
- 금액·기간 단정 주의: 세법 수치는 세무사 확인 후에만 구체적으로
- 확정 수치: 500여 곳 / 세무사 2명(경력 합산 30년: 한진식 2010~, 차정범 2014~) / 직원 10명 / 평균 근속 6.7년(근속 1년 미만 제외) / 50여 가지 체크리스트
- 지역 표기: 본문은 "서울 여의도", 푸터·구조화 데이터는 정식 주소(영등포구) — 의도된 이중 표기
- 고객 노출 브랜드는 "한택스" (푸터에 나이스원세무법인 스마트지점 고지)

## 개인정보 (엄수)
- 화면 캡처 게재 전 반드시 검수: 고객 상호·이름·연락처·사업자번호는 픽셀 처리
- 직원 얼굴 사진 게재 금지(본인들 의사) — 일러스트+실명만. 퇴사자 발생 시 카드 즉시 제거
- 원본(미처리) 캡처 파일을 저장소에 커밋 금지

## FAQ 추가 방법
`faq/index.html`의 `<details>` 블록 추가 + 상단 FAQPage JSON-LD의 mainEntity에 동일 문구로 1건 추가 (본문과 JSON-LD 불일치 금지)
